# NOVA v2 MCP Server dependencies
mcp[cli]>=1.0.0
pydantic>=2.10.1               # transitive floor from mcp>=1.2.0
sentence-transformers>=2.2.0  # local embeddings, no API key required
python-dotenv>=1.0.0
filelock>=3.12.0
ijson>=3.3.0
pyarrow>=14.0.0               # arrow_cache.py columnar projection (MUNINN + NÓTT fast path)
numpy>=1.23.0                 # vectorised cosine / decay math (also pulled in by sentence-transformers)

# Model clients — required by ravens.py, Gemini/gemini_mcp.py, utilities/autoresearch.py
anthropic>=0.30.0       # HUGINN + MUNINN retrieval lanes
google-genai>=0.3.0     # Gemini Flash implementation lane + autoresearch

# Ternary epistemic memory PoC — mcp/ternary_net.py + utilities/{export,train}_ternary_net.py
# Optional: tests skip gracefully when torch is absent.
torch>=2.1,<2.5         # CPU wheel is sufficient for the PoC
